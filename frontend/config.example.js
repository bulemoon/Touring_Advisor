// 复制本文件为 config.js 并填写真实值
// config.js 已在 .gitignore，不会被提交到 Git

const CONFIG = {
  // Railway 部署时改为 Railway 提供的后端 URL
  // 本地开发使用 http://localhost:8000
  API_BASE_URL: '',

  // 高德地图 Web JS API Key
  // 注册 lbs.amap.com -> 控制台 -> 应用管理 -> 获取 Key -> 选择「Web 端(JS API)」
  AMAP_JS_KEY: '',
};
